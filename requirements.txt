# Core
pydantic>=2.0
fastapi>=0.110
uvicorn[standard]>=0.29

# Stage A
# (regex is stdlib — no extra packages needed)

# Stage B — SVM (default offline backend)
scikit-learn>=1.4
scipy>=1.12

# Stage B — BERT (optional; install if corpus >= 500 rows)
# transformers>=4.40
# torch>=2.2

# Testing
pytest>=8.0
httpx>=0.27          # FastAPI TestClient dependency

# Utilities
pandas>=2.2
python-multipart     # FastAPI file uploads (optional)
